<?php

return [
    /*
    |--------------------------------------------------------------------------
    | 公開レベルに関する設定
    |--------------------------------------------------------------------------
    |
    */
    // 公開レベル毎のラベル定義
    'public_level_labels' => [
        0 => '自分',
        2 => '友達',
        5 => '会員',
        10 => '全員'
    ],

    /*
    |--------------------------------------------------------------------------
    | プロフィールに関する設定
    |--------------------------------------------------------------------------
    |
    */
    // プロフィールとユーザとの関連付けタイプ（aggregationまたはcomposition）
    'profile_user_relation_type' => 'aggregation',

    // プロフィールデフォルト順位（oldestまたはlatest）
    'profile_default_order' => 'oldest',

    /*
    |--------------------------------------------------------------------------
    | コンフィグに関する設定
    |--------------------------------------------------------------------------
    |
    */
    // カスタムコンフィグクラス定義
    'config_value_objects' => [
        // ここにカスタムコンフィグクラスを定義することで、コンフィグの値を取得できます。
        // 例:
        // 'custom_config' => \App\CustomConfig::class,
    ],

    /*
    |--------------------------------------------------------------------------
    | 写真に関する設定
    |--------------------------------------------------------------------------
    |
    */
    // 写真タイプマッピング
    'photo_types' => [
        // ここに写真タイプと評価文字列（正規表現）のペアを定義します。
        // 例:
        // 'google' => '/^(?:https?:\/\/)?photos\.google\.com\//',
        // 'amazon' => '/^(?:https?:\/\/)?www\.amazon\.com\/photos\//',
    ],

    /*
    |--------------------------------------------------------------------------
    | 場所に関する設定
    |--------------------------------------------------------------------------
    |
    */
    // 緯度の精度
    'location_latitude_precision' => 7,

    // 経度の精度
    'location_longitude_precision' => 7,

    /*
    |--------------------------------------------------------------------------
    | その他に関する設定
    |--------------------------------------------------------------------------
    |
    */
    // HTMLキャストフック
    'html_cast_hooks' => [
        // フックの定義
        // 例: \App\Hooks\CustomHtmlHook::class,
    ],

    // URLキャストフック
    'url_cast_hooks' => [
        // フックの定義
        // 例: \App\Hooks\CustomUrlHook::class,
    ],
];
